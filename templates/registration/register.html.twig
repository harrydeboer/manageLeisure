{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    {% for message in app.flashes('verify_email_error') %}
        <div class="flash-notice">
            {{ message }}
        </div>
    {% endfor %}
    <h1>Register</h1>

    {{ include('registration/form.html.twig', {form: form, submitLabel: 'Register'}) }}
{% endblock %}
